/**
 * ErrorMessage Component
 */
import React from 'react';
import styles from './styles.css';

const ErrorMessage = () => (
  <div className={styles.message}>
    Sorry, something is wrong on our end. Try again later.
  </div>
);

export default ErrorMessage;
